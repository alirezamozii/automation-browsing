# Automation Platform — Python dependencies
# Pinned to minimum compatible versions.
# Setup.bat checks for outdated packages and prompts to update.

playwright>=1.44.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
aiosqlite>=0.20.0
pydantic>=2.7.0
jinja2>=3.1.4
python-multipart>=0.0.9
